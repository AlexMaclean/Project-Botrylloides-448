#!/bin/bash

CONF=kmer.conf
K=30

sed -i "s/GRAPH_KMER_SIZE =.*/GRAPH_KMER_SIZE = $K/g" $CONF

#masurca $CONF || exit 1

#DATE=$(date +"-%m-%d-%H-%M")
#OUT_PATH="${CONF%%.*}$DATE"

#mkdir $OUT_PATH
#mv assemble.sh  $OUT_PATH/
#cd $OUT_PATH

#./assemble.sh
